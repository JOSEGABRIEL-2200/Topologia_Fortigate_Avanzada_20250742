Este backup (18 de septiembre, etapa intermedia) fue reemplazado por la version final:

    FortiGate_running-config_2026-09-22.conf

Se conserva unicamente esta nota para trazabilidad del proceso de configuracion.
El archivo final incluye las politicas completas (USUARIOS_WEB_HTTPS con DPI +
IPS_SQLI_CUARENTENA + File Filter, BLOQUEAR_WEB_DB_RESTO, DOS_USUARIOS_WEB) que
en este snapshot intermedio aun no existian.
